// 跨模块路由名枚举 —— 真实语料里 63% 的跳转实参走这种形式
export enum RouterMap {
  // 首页
  HOME_PAGE = 'HomePage',
  // 订单详情
  ORDER_DETAIL = 'OrderDetail',
  // 订单列表
  ORDER_LIST = 'OrderList',
}

export enum PageName {
  FeedbackSubmit = 'FeedbackSubmitPage',
}
